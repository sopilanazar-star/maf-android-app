<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/stadium_maf">

    <!-- 🎯 ГЕРБ ПО ЦЕНТРУ -->
    <ImageView
        android:id="@+id/centerLogo"
        android:layout_width="220dp"
        android:layout_height="220dp"
        android:src="@drawable/maf_logo"
        android:layout_centerInParent="true"
        android:alpha="0" />   <!-- він з'явиться плавно -->

    <!-- ⚽ М'яч для анімації -->
    <ImageView
        android:id="@+id/ballView"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/ball_maf"
        android:layout_centerVertical="true"
        android:translationX="-400dp" />

    <!-- ✨ Текст, який з'явиться після м'яча -->
    <TextView
        android:id="@+id/mafText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="МИКОЛАЇВСЬКА АСОЦІАЦІЯ ФУТБОЛУ"
        android:textColor="#FFFFFF"
        android:textSize="28sp"
        android:textStyle="bold"
        android:alpha="0"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/centerLogo"
        android:layout_marginTop="20dp"/>

</RelativeLayout>
