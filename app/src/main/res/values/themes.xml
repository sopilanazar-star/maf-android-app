<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Основна тема додатку -->
    <style name="Theme.MAFFootball" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Кольори статус-бара -->
        <item name="android:statusBarColor">@android:color/transparent</item>

        <!-- Прибираємо splash Android 12 повністю -->
        <item name="android:windowSplashScreenBackground">@android:color/black</item>
        <item name="android:windowSplashScreenAnimatedIcon">@null</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="android:windowSplashScreenAnimationDuration">0</item>

        <!-- НІЯКИХ лого, НІЯКИХ фонів, НІЯКИХ зображень -->
        <!-- Усе інтро ми показуємо самі через IntroActivity -->
    </style>

</resources>

